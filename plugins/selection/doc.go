// Package selection turns mouse input into a Selected tag on world
// entities - left-click to select, drag for a marquee, shift to add.
// WithRenderer draws an outline on every Selected entity and the live
// drag marquee; write your own HandleEvents against State for a
// different binding scheme.
package selection
